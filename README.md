# sanjaycj.com

My personal site, built with Astro and Tailwind.

```sh
npm install
npm run dev      # localhost:4321
npm run build    # builds to ./dist
```

Content lives in `src/data/` — `site.ts` (bio, experience, links) and `workshop.ts` (projects). Deployed on Vercel.
